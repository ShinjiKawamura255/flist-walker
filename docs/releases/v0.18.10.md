# v0.18.10 Release Record

## Release
- Date: 2026-06-14
- Status: Published
- Release: https://github.com/ShinjiKawamura255/flist-walker/releases/tag/v0.18.10
- Published at: `2026-06-13T18:24:21Z`
- Latest release API confirmed `v0.18.10` with 24 assets.
- Public `SHA256SUMS` download returned HTTP 200.

## Immutable Tag
- Annotated tag object: `e56a2d6d10bcf2048f4f4cf2ed782131fd99049d`
- Peeled commit: `7c0734bbe3b524d577efa950fb877027044c26cc`
- Local and remote tag object and peeled commit values matched before publication.

## CI And Workflow
- Warning repair confirmation: https://github.com/ShinjiKawamura255/flist-walker/actions/runs/27466994937
- Final preparation CI: https://github.com/ShinjiKawamura255/flist-walker/actions/runs/27467181494
- Tagged release workflow: https://github.com/ShinjiKawamura255/flist-walker/actions/runs/27474581585
- Linux, macOS, Windows native, Windows GNU cross, Clippy/coverage, Cargo Audit, release preflight, four release builds, signing, bundle validation, and draft creation succeeded.
- The final preparation and tagged macOS logs contained no FlistWalker, dependency, future-incompatibility, or compiler warnings.

## Release Assets
- Linux x86_64: standalone binary, `tar.gz`, README, LICENSE, and THIRD_PARTY_NOTICES.
- Windows x86_64: standalone EXE, ZIP, README, LICENSE, and THIRD_PARTY_NOTICES.
- macOS x86_64 and arm64: standalone binary, app ZIP, `tar.gz`, README, LICENSE, and THIRD_PARTY_NOTICES.
- Integrity: `SHA256SUMS` and `SHA256SUMS.sig`.
- Total: 24 assets, including exactly 22 checksum entries and a 64-byte detached signature.
- `scripts/validate-release-bundle.sh v0.18.10` passed against a fresh download of the draft assets.
- All 22 checksums passed. Archive and standalone sidecars contained the required LICENSE and THIRD_PARTY_NOTICES files.
- The signing step required the signing-key-derived, configured, and embedded public keys to match, then verified the signature with both configured and embedded keys before writing it.
- Raw `.app` directories were not uploaded. The documented `*-app.zip` distribution assets were included.

## Native And GUI Validation
- `cargo test --locked`: 504 tests, 498 passed, 6 ignored.
- `cargo clippy --all-targets -- -D warnings`, `cargo audit`, formatting, four-target metadata, and Windows GNU target checks passed.
- `scripts/test-build-rust-win.ps1`, `scripts/build-rust-win.ps1 -NoInstall`, and `scripts/test-windows-build-artifact.ps1` passed.
- Windows GNU FlistWalker 0.18.10 launched successfully. `Browse...` opened the native `Open a Folder` dialog at the configured root, and `Escape` canceled it cleanly.
- Detailed GUI evidence: `docs/releases/evidence/v0.18.10/GUI-TESTREPORT.md`.
- Saved-root and FileList GUI coverage is inherited from `docs/releases/evidence/v0.18.9/`.
- Interactive macOS GUI validation was SKIPPED because no macOS desktop was available. Native macOS CI tests and both tagged release builds passed as substitute evidence.

## Warning Decision
- The only tagged workflow warning was a Node `Buffer()` deprecation emitted inside GitHub's `actions/download-artifact`.
- It was not emitted by FlistWalker, Rust, a packaged dependency, or the generated assets.
- Both specialist reviewers approved it as nonblocking after the signed bundle and downloaded assets passed validation.

## Review
- Testing/release-integrity review: No blocking findings.
- Security/operability review: No blocking findings.
- The release body contains Security and Known issues sections, states that macOS artifacts are not notarized, and contains none of the development-only update override environment variable names.
- `docs/RELEASE_INCIDENT_RUNBOOK.md` remains the containment procedure for a post-publication incident.

## Superseded Candidate
- `v0.18.9` remains an unpublished draft release candidate with 24 assets.
- Its tag and assets were not rewritten or overwritten.
- Rejection evidence: `docs/releases/v0.18.9-candidate-rejection.md`.
