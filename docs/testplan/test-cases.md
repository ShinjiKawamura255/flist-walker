# Test Cases

## Test cases
| TC ID | Level | Purpose | Related SP |
| --- | --- | --- | --- |
| TC-001 | unit | `FileList.txt` / `filelist.txt` の優先検出 | SP-001 |
| TC-002 | unit | リスト未存在時に walker 走査で候補生成 | SP-002 |
| TC-003 | unit | ファジー検索が関連度順・件数制限を満たす | SP-003 |
| TC-003A | unit | 通常語の各 token は subsequence-only よりリテラル一致を高順位にする | SP-003 |
| TC-003B | unit | 同じ unanchored 完全一致 token を複数回指定した場合は、指定回数以上のリテラル出現を含む候補だけが一致する | SP-003 |
| TC-004 | unit | 実行可能ファイルで execute 分岐 | SP-004 |
| TC-004A | unit | open / execute helper seam が open/execute handler を適切に呼び分ける | SP-004, SP-005 |
| TC-005 | unit | フォルダで open 分岐 | SP-005 |
| TC-006 | integration | legacy `--cli`/`--root`/`--limit` invocation と既定 no-match exit 0を維持し、承認済みの `[score] absolute-path` からroot相対path-only改行出力へのmigration、およびCLI-only optionと`--interactive`が`--cli`を要求することを検証する | SP-006 |
| TC-006A | integration | CLI の `--limit` は 1000 件へ暗黙丸めせず、absolute/NUL/no-match/type/regex/case/source/ignore/progress option と conflict/error を規範どおり扱う | SP-006 |
| TC-007 | perf | 10万件検索の遅延計測 | SP-007 |
| TC-008 | unit | 例外時に非ゼロ終了コードを返す | SP-008 |
| TC-009 | unit | モジュール分離により単体テスト可能 | SP-009 |
| TC-010 | manual+unit | GUI で検索/選択/実行/再読込が行える（`docs/GUI-TESTPLAN.md` の `GSM-001` から `GSM-006`） | SP-010 |
| TC-011 | manual | GUI 回帰手順に基づく再検証が可能（`scripts/gui-smoke-fixture.sh` と `docs/GUI-TESTREPORT.template.md` を使用） | SP-011 |
| TC-012 | unit | プレビューキャッシュが上限件数を超えない | SP-007 |
| TC-012A | unit | `.vbs` など非 `.txt` でも UTF-8 / UTF-16 / 主要レガシー文字コードなら本文プレビューでき、バイナリは unreadable を維持する | SP-010 |
| TC-012B | unit | File/Directory Preview は `Updated` と取得可能な `Created` を UTC 形式で表示し、Read-only / Windows Hidden 属性と symlink の Target および Target metadata を条件付きで表示する | SP-010 |
| TC-013 | manual | インデックス中に空クエリ一覧が逐次更新され、操作が継続できる | SP-002, SP-007 |
| TC-014 | unit | FileList 作成の成功/失敗応答で GUI 状態遷移が正しい | SP-010, SP-008 |
| TC-015 | integration | CLI 非空クエリ出力形式と root 不正系を検証する | SP-006, SP-008 |
| TC-016 | unit | Root 変更時に旧 Root 由来の選択状態（current/pinned/preview）がクリアされる | SP-010 |
| TC-017 | unit | Root 変更時に旧 Root 向け FileList 上書き確認が破棄される | SP-010 |
| TC-018 | unit | `Ctrl+Shift+C`（macOS では `Cmd+Shift+C`）は、backend が `Key::C` または `Event::Copy` のどちらで通知しても検索窓フォーカス時に結果パスを優先コピーする | SP-010 |
| TC-019 | unit | 大規模 FileList 解析はストリーミングで中断可能（新 request で supersede） | SP-001, SP-007 |
| TC-020 | manual+unit | Windows: 異解像度モニタ跨ぎ時に復元サイズが破綻せず、IME `Space` / `Shift+Space` で最低限の半角スペースが検索窓へ反映される（`CompositionUpdate` 同フレームでも挿入される） | SP-010, SP-011 |
| TC-021 | unit | 検索窓フォーカス中でも `Ctrl+N` / `Ctrl+P` が current row を移動する | SP-010 |
| TC-022 | unit | 検索窓フォーカス中でも `Ctrl+G` / `Esc` で query clear + filter reset が実行される | SP-010 |
| TC-023 | unit | `Tab` / `Shift+Tab` はフォーカス非依存で PIN 固定/解除を実行し、既定では current row を維持する。`tab_pin_moves_to_next_row=true` では PIN 固定/解除後に次行へ移動する | SP-010, SP-016 |
| TC-023A | unit | tab切替、closed-tab/session restore、`Esc`系reset後も、空Resultsは`None`、非空Resultsは範囲内のvisible current rowとなる。inactive compacted tabだけはbase resultsに対するselection保持を許す | SP-010 |
| TC-024 | unit | IME スペースフォールバックと composition commit fallback はカーソル位置へ挿入し、挿入後カーソルへ更新する | SP-010 |
| TC-025 | unit | FileList ストリーミング時に種別不明候補を先行表示し、LINK identity とリンク先種別を分離して、解決後に FILE/DIR/LINK 表示とフィルタ状態を反映する。OTHER/解決不能の終端状態は再解決しない | SP-001, SP-010, SP-007 |
| TC-026 | unit | 回帰: 検索窓フォーカス中でも `ArrowUp` / `ArrowDown` で current row が移動する | SP-010 |
| TC-027 | unit | 回帰: `Ctrl+I` は検索窓フォーカス有無を問わず PIN をトグルし current row を維持する | SP-010 |
| TC-028 | unit | 回帰: 検索窓フォーカス中でも `Ctrl+J` / `Ctrl+M` が `Enter` 同等に実行/オープンを起動する | SP-010 |
| TC-029 | unit | プロセス shutdown フラグが set/clear 可能で、シグナル経由の終了要求を保持できる | SP-010, SP-008 |
| TC-030 | unit+regression | 階層 FileList 展開は候補集合中の `FileList.txt` / `filelist.txt` 完全一致エントリのみを対象にし、単一 discovery 後も root と検出済み FileList を同じ canonical path basis で比較して新しい nested FileList の subtree/depth override を維持し、子 FileList 解析中も supersede で中断できる | SP-001, SP-007 |
| TC-031 | unit | indexing 中の非空クエリ自動再検索は小差分では抑制し、十分な差分蓄積時のみ再検索する | SP-007, SP-010 |
| TC-032 | unit | GUI 終了時の worker join はタイムアウト付きで、完了系/タイムアウト系の両方を検証する | SP-008, SP-010 |
| TC-033 | unit | `Ctrl+R` で履歴検索モードを開始し、履歴候補を最新順で表示する | SP-010 |
| TC-034 | unit | query 履歴は空文字を保存せず、同一 query の再登録時は既存項目を除去して最新位置へ移動する | SP-010 |
| TC-035 | unit | query 履歴はタブ切り替え後も全タブで共通に参照できる | SP-010 |
| TC-036 | unit | Root 変更時は query 履歴の参照位置のみリセットし、履歴本体は保持する | SP-010 |
| TC-037 | unit | 連続打鍵中は query 履歴を保留し、入力区切り後に最終 query だけを保存する | SP-010 |
| TC-038 | unit | IME 合成中の未確定文字列は query 履歴へ保存せず、確定後の query のみを保存する | SP-010 |
| TC-039 | unit | 保存済み `last_root` / `default_root` / tab root は root 存在確認を起動後へ遅延し、active tab index をクランプする | SP-010 |
| TC-040 | unit | タブ復元時は active tab の root/query/filter を復元し、起動直後の index は active tab だけ開始する | SP-010 |
| TC-041 | unit | 復元された background tab は初回 activate 時にだけ lazy refresh を開始する | SP-010 |
| TC-042 | unit | 履歴検索中の `Enter` / `Ctrl+J` / `Ctrl+M` は選択中履歴を query に展開する | SP-010 |
| TC-043 | unit | 履歴検索中の `Esc` / `Ctrl+G` は開始前 query を復元してキャンセルする | SP-010 |
| TC-044 | unit | `Ctrl+Shift+R` は履歴検索を開始しない | SP-010 |
| TC-045 | unit | query 履歴は 100 件上限を維持し、保存状態へ永続化される | SP-010 |
| TC-046 | unit | runtime config の `restore_tabs_enabled` が有効な間は `Set as default` が無効化され、既定 root を保存しない | SP-010 |
| TC-046A | unit | `Manage list` はメインウィンドウ外枠を基準に中央配置し、負座標を維持し、親 geometry がない場合は OS 配置へフォールバックする。通常時の一覧横幅全体をクリック可能な単一行選択から root path を編集でき、編集中に変更がなければ別行クリックで編集解除・選択、別行ダブルクリックで編集対象切替を行い、変更済みなら `Save` / `Cancel` まで切替を拒否する。無効・重複パスを拒否して編集を継続する。追加・編集の検証失敗は対象入力欄の専用エラー、再フォーカス、全選択要求として保持し、入力変更時にその欄のエラーだけを解除する。削除モード中だけチェックボックスによる複数選択を許可し、削除モードの `Cancel` は draft を変更しない。追加・編集・削除は `Apply` / `OK` でのみ保存済み root list へ反映し、ウィンドウの `Cancel` では破棄する。default root の編集は新パスへ追従し、削除は default root を解除する | SP-010 |
| TC-047 | unit | FileList 作成時は祖先の既存 FileList へ子 FileList 参照を重複なく追記し、親 mtime を維持する | SP-001 |
| TC-048 | unit | 祖先探索は callback/失敗時に即停止し、それ以降の上位階層を処理しない | SP-001 |
| TC-049 | unit | FileList に root 外パスが含まれても候補表示時の追加フィルタを行わず、インデクシング経路の挙動を維持する | SP-007 |
| TC-050 | unit+integration | UI precheck は filesystem I/O なしで、`.`、root 内の正規化可能な component を `Defer` し、root を越える `..` または明らかな別 prefix だけを `Reject` する。worker は trusted root と direct/open-containing-folder の全 effective target を解決済み component で事前認可し、通常 path、root 外 path、`..` escape、解決不能 root/target、mixed valid/invalid、root 外を指す link を検証する。open-containing-folder は通常 file/directory、file/directory link、broken link、metadata error を区別し、推測による親変換を行わない。事前認可失敗時の recording backend 呼び出しは 0 件とし、成功時は display path ではなく最後に認可した解決済み execution path だけを受け取る。各 source の種別/effective target を直前に再導出し、種別変更または再検証失敗では残件を停止して先行呼び出し件数を含む partial completion を返す。blocked/executor failure/partial の利用者向け通知は display path を保ち、canonical execution path と OS error 本文を含めない | SP-004 |
| TC-051 | unit+manual | platform path evidence として、Unix symlink escape、作成可能な Windows symlink/junction escape、Windows の大小文字・extended/verbatim prefix、UNC の同一 root 配下/同一 share の root 外/別 share を検証する。root 外を解決先とする directory link/junction の direct/open-containing-folder は backend 0 件で拒否し、root 内 file link の open-containing-folder は解決済みの字句的親だけを許可する。同一 UNC root 配下は許可し、別 share/root 外と解決不能 UNC は拒否する。Windows junction と real UNC は `manual-regression-traceability.md` の手順・expected/actual・実行可否を記録し、未実行を成功扱いにしない | SP-004 |
| TC-052 | unit | Create File List で祖先 FileList への追記が実際に発生しうる場合は確認ダイアログを要求する | SP-001, SP-010 |
| TC-053 | unit | 祖先追記確認を拒否した場合、root 直下の FileList 作成だけを継続する | SP-001, SP-010 |
| TC-054 | unit | runtime config の `history_persist_disabled` が有効なとき query history を保存も復元も行わない | SP-010 |
| TC-055 | manual | README / release docs / release template に平文 history 保存、checksum 検証、notarization の暫定運用と `Security` / `Known issues` 記載前提が明記されている | SP-010, SP-012 |
| TC-056 | integration+regression | required CI は version-addressed Linux/macOS/Windows、固定 Rust/Action/tool、限定 cache、timeout/concurrency で test/build/clippy/coverage を実行し、単一 `CI Gate` が結果を集約する。全変更がallowlisted documentation `A`/`M`の場合だけheavy jobをすべてskipし、rename/delete/unknown/base不明/diff失敗は全実行へfail closedする。GNU updater E2Eは専用GNU producerだけを待ち、native matrixを直列待機しない | SP-012 |
| TC-056A | docs+security | `cargo audit` の accepted transitive warning は `docs/OSS_COMPLIANCE.md` に依存経路、受容理由、owner、review cadence、再評価 trigger を記録する | SP-012 |
| TC-056B | integration+security | 任意階層の Cargo manifest/lock、`rust/.cargo/audit.toml`、audit workflow、CI policy checker/test の変更は audit を実行して `CI Gate` へ集約する。対象変更で skipped は失敗、非対象変更で skipped は成功とする | SP-012 |
| TC-056C | scheduled+operations | scheduled audit と latest canary は required gate から分離し、失敗時に専用 issue を作成または更新する。audit は24時間、canary は7日以内のagent triageへ接続する | SP-012 |
| TC-056D | unit+integration | CI policy checker は latest alias のrequired使用、非SHA Action、未固定Rust/tool、target/tool cache、timeout/concurrency/permissions違反、guardian以外の`pull_request_target`、audit skip契約違反、Dependabot運用欠落を拒否する。Dependabot merge登録は正確な`--auto --rebase --delete-branch`を1回だけ要求し、merge/squash/複数登録を拒否する | SP-012 |
| TC-056E | operations | `master` はPR、approval 0、GitHub Actions由来の`CI Gate`と`CI Policy Guardian`、force/delete禁止、linear history、rebase-only、merge済みremote branch自動削除を要求する。各変更は、clean な `master` が fetch/FF-only pull 後に `origin/master` と一致することを確認してから、最初のcommit前にfeature branchで開始する。auto-merge 実完了後、clean worktree の active task だけが `master` を fast-forward 同期し、現在 branch と `master == origin/master` を確認する。マージ済みかつ未使用の local feature branch は通常削除し、rebaseでSHAが書き換わり通常削除だけが拒否された場合だけ、記録済み PR 番号の `MERGED`/`mergedAt`/base `master`/exact head 一致、対象が `master` 以外、worktree未使用、branch固有merge commitなし、patch等価性を全て確認して限定 `-D` cleanup を許可する。dirty worktree、未マージまたはPR不一致、分岐した `master`、fast-forward不能、patch差分、branch merge commit、`master`対象、または他 worktree が使用する branch では開始・後処理が停止する。2commit以上のmachine PRがadmin bypassなしでrebase auto-mergeされ、境界・順序・message・author・patch/tree対応を保ち、追加commitが各1 parentでmerge commitを含まないことを確認する | SP-012 |
| TC-056F | unit+integration+security | Guardianはdefault branchのtrusted checkerだけを実行し、PR policy blobをdataとして検査する。workflow/Dependabot/toolchain/audit exception/checker/testの構造変更、PR head実行、write権限、secret/cache/artifact利用をfail-closedで拒否し、runner/action/tool version pinだけを許可する | SP-012 |
| TC-057 | unit | `Score` / `Name` / `Modified` / `Created` / `Size` のソートモード遷移と `Score` 復帰を検証する | SP-013 |
| TC-057A | unit | 検索応答は limit 前の全マッチ件数を返し、GUI snapshot / status は表示件数と全マッチ件数を区別できる | SP-010, SP-013 |
| TC-057B | unit | `All matches` scope の非 `Score` ソートは検索 worker に再要求され、表示中 snapshot 外の候補も全マッチ集合の sort key 上位なら表示対象になる | SP-013 |
| TC-058 | unit | query 変更や結果更新時にソートを破棄し、インデクシング経路へ属性取得を追加しない | SP-013 |
| TC-059 | unit | 日付ソートの未キャッシュ path は別 worker へ要求し、古い応答は request_id で破棄する | SP-013 |
| TC-060 | unit | sort metadata cache は上限を超えて増加せず、古い項目を破棄する | SP-013 |
| TC-061 | unit | `created()` 取得不可の項目は `Created` ソート時に末尾へ送り、フォルダや size 取得不可項目は `Size` ソート時に末尾へ送る | SP-013 |
| TC-062 | manual | 結果ペインの `Sort` UI から各ソートを選択でき、`Modified` / `Created` / `Size` 中も入力が継続できる | SP-010, SP-013 |
| TC-063 | perf | ソート機能追加後もインデクシング時の属性取得数を増やさず、既存の逐次表示挙動を維持する | SP-007, SP-013 |
| TC-064 | unit | タブのドラッグ移動は並び順のみを更新し、active tab の実体と保存順を維持する | SP-010 |
| TC-068 | unit | 回帰: 検索・sort・filter・非同期応答によるResults更新は、従来行を末尾へ丸め、従来行がなければ0行目を選択し、空なら`None`にする | SP-010 |
| TC-069 | unit | 回帰: `Ignore Case` は既定で有効で、無効化時は検索結果とハイライトが case-sensitive になる | SP-010 |
| TC-070 | unit | 回帰: 起動直後、query reset、preset、tab/session restore後も、候補が現れた時点で1行目が既定選択になり次描画で見える | SP-010 |
| TC-071 | unit | 大規模候補でも partial top-N の結果が full ranking の先頭と一致する | SP-003, SP-007 |
| TC-072 | unit | 並列検索の収集結果は逐次検索と同じ ranking を返す | SP-003, SP-007 |
| TC-073 | unit | 非アクティブタブの結果キャッシュ compact 後も、再表示時に current row と結果一覧を復元できる | SP-010 |
| TC-074 | unit | GitHub Releases の latest 応答から現在 platform の更新 asset と sidecar 文書（README / LICENSE / THIRD_PARTY_NOTICES）と `SHA256SUMS` を選択できる | SP-014, SP-017 |
| TC-075 | unit | staged binary と sidecar 文書は `SHA256SUMS.sig` の署名検証と `SHA256SUMS` の checksum 検証を通過して private verified bundle になった場合のみ自己更新へ進む | SP-014 |
| TC-076 | unit | Windows/Linux の自己更新は同一ディレクトリへ create-new copy した Rust helper を完全一致の hidden 引数で起動し、registration/acknowledgement 前は installation target を変更せず、実行中 binary の直接上書きを避ける | SP-014 |
| TC-077 | unit | macOS は新版検知時も自動置換へ進まず、手動更新案内に留める | SP-014 |
| TC-078 | unit | 更新確認/ダウンロード失敗は notice に反映されても GUI 操作を継続できる | SP-014 |
| TC-079 | manual+unit | 手動試験 override により同一 version でも更新ダイアログを表示できる | SP-014 |
| TC-080 | manual+unit | 手動試験 override により downgrade 候補でも更新ダイアログを表示できる | SP-014 |
| TC-081 | unit | 更新ダイアログで抑止した target version は起動間で保持され、より新しい version が出るまで再表示されない | SP-014 |
| TC-082 | unit+perf | 回帰: FileList の `\` 区切り候補を filesystem existence probe なしでプラットフォーム優先解釈し、line-only fast path が metadata-probe baseline を十分に上回ることを維持する（CI 下限 1.20x）。FileList read path は行バッファを再利用し、`lines()` による行ごとの確保 baseline より遅くならないことを明示計測する | SP-001, SP-007 |
| TC-083 | unit+perf | Walker 初期インデクシングは通常ファイル/ディレクトリを `file_type` ベースで流し、特殊ファイルを LINK に誤分類せず、symlink のリンク先判定だけを遅延する。eager metadata 解決に対して現行 control baseline で 1.25x 以上の速度差を維持し、その後に遅延種別解決を自動開始する | SP-002, SP-007 |
| TC-084 | unit | Source が FileList のタブで Create File List を確認すると、新規タブを開かずに同一タブの裏で Walker indexing を実行する | SP-001, SP-010 |
| TC-085 | unit | FileList source の Create File List 完了時、元タブが background 化していてもその元タブを再インデックスし、完了前に元タブ root が変わっていた場合は再インデックスしない | SP-001, SP-010 |
| TC-086 | unit | 実行バイナリと同一ディレクトリに `FLISTWALKER_DISABLE_SELF_UPDATE` ファイルがある場合も、自己更新を無効化する | SP-014 |
| TC-087 | unit | Create File List の保留状態は status panel または確認ダイアログ経由でキャンセルできる | SP-001, SP-010 |
| TC-088 | unit | Create File List 実行中のキャンセル要求は cancel flag を立て、`Canceled` 応答で状態と notice を解放する | SP-001, SP-010 |
| TC-089 | unit | Create File List の cancel 済み request は root 直下の既存 `FileList.txt` を置換しない | SP-001 |
| TC-090 | unit | 回帰: GNU Windows ビルドは `resource.o` を最終 `flistwalker` バイナリへ明示リンクし、Explorer アイコン欠落を防ぐ | SP-012 |
| TC-091 | unit | 回帰: GUI 終了時の worker join timeout は短時間に保たれ、close が不要に 2 秒近く遅延しない | SP-008, SP-010 |
| TC-092 | unit | 回帰: shared query module により search と highlight が exact / OR / anchor の query 解釈を一致させる | SP-003, SP-010 |
| TC-093 | unit | regex モードでも plain token / plain OR token は fuzzy 条件で一致し、regex 構文 token だけ regex として評価される | SP-003 |
| TC-094 | unit | タブ accent color は保存・復元時に維持される | SP-010 |
| TC-095 | unit | tab accent palette は light/dark theme で DropSendTo の Slot Color と同じ RGB を返す | SP-010 |
| TC-096 | unit | 起動時更新確認失敗は失敗ダイアログへ反映され、通常操作継続のため pending/in_progress を解放する | SP-014 |
| TC-097 | unit | 起動時更新確認失敗ダイアログの抑止設定は UI state に保存され、抑止中は失敗ダイアログを再表示しない | SP-014 |
| TC-098 | unit | `FLISTWALKER_FORCE_UPDATE_CHECK_FAILURE` はネットワーク非依存で起動時更新確認失敗を強制し、失敗ダイアログ抑止中でも表示できる | SP-014 |
| TC-099 | manual | structural refactoring 後も GUI の主要操作が連続利用に耐える（`GSM-001` から `GSM-010`） | SP-010 |
| TC-100 | unit | self-update candidate 解決は release asset 選択と support classification を分離し、manual-only fallback を契約として保持する | SP-014 |
| TC-101 | unit | update request / install transitions emit trace commands for supportability and retain request_id correlation | SP-014 |
| TC-102 | unit | Create File List の stale requested root completion は cleanup だけを行い、`use_filelist` 復帰や notice 更新を行わない | SP-001, SP-010 |
| TC-103 | unit | Root 変更時に旧 Root 向け FileList の祖先追記確認と Walker 利用確認が破棄される | SP-001, SP-010 |
| TC-104 | unit | render command 経由の tab close / reorder は active tab identity と queue cleanup を壊さない | SP-010 |
| TC-105 | unit | root dropdown selection は popup を閉じて選択 root を適用し、後続の root cleanup flow を開始できる | SP-010 |
| TC-106 | unit | background response routing は active tab を巻き戻さず、tab close 時は request routing cleanup だけを行う。background search/sortのselected path変更はNone→Someを含め旧preview/pending routingを無効化し、非compaction inactive tabが完了previewを破棄した場合もtab scoped reloadを記録する。activationはcompactionまたは明示flag時だけ新current pathを既存async/cache workerへ要求し、遅延旧responseを拒否する。session restoreのlazy refresh併発時はrefresh後のkind resolution/preview requestがlive ownershipを保ち一度だけsettleする | SP-010 |
| TC-107 | unit | `ui_model` の preview text は action policy を埋め込まず、実行可否は `actions.rs` 側の責務に留める | SP-004, SP-010 |
| TC-108 | ci | `cargo llvm-cov` の line coverage gate は 75% 未満への退行を CI で失敗させ、`lcov.info` artifact を継続生成する | SP-012 |
| TC-109 | docs | GitHub Issue template と `docs/SUPPORT.md` は、version / OS / launch mode / reproduction / redaction を求めつつ、既定 telemetry や自動 crash upload を導入しない supportability 導線を固定する | SP-010, SP-012 |
| TC-110 | unit | ignore list ファイルの候補は既定有効で除外され、GUI の `Use Ignore List` チェックボックスで有効/無効を切り替えられる | SP-015 |
| TC-111 | unit | runtime config file が無い初回起動では current env を seed にした `~/.flistwalker/.flistwalker_config.json` が自動生成され、既存 file がある場合は env 変更より file 内容が優先される | SP-016 |
| TC-112 | unit | 回帰: `Files` / `Folders` 両方有効の高速経路でも ignore list は省略されず、`old` / `~` を literal に含む候補が結果へ戻らない | SP-015 |
| TC-113 | unit | 起動時初期化は embedded ignore sample を `flistwalker.ignore.txt.example` として生成し、既存ファイルを上書きしない | SP-017 |
| TC-114 | unit | embedded ignore sample の説明文には `flistwalker.ignore.txt` へリネームして live ignore list として使う案内が含まれる | SP-017 |
| TC-115 | unit | Windows では runtime config / UI state / saved roots / window trace の保存先が `%LocalAppData%\flistwalker\` になり、Linux/macOS では `~/.flistwalker/` に戻る | SP-016 |
| TC-116 | unit | Windows の旧 exe-side / home-directory 配置ファイル、Linux/macOS の旧 home-directory 直下ファイルは、新保存先に同名 file が無い場合だけ移行され、既存の新配置 file を上書きしない | SP-016 |
| TC-117 | unit | `!` 除外と ignore list は fuzzy fallback を使わず、literal / anchor 一致だけで除外し、`Ignore Case` の有効/無効に従う | SP-015 |
| TC-118 | unit | Windows の一般 `.ps1` は Execute ではなく Open 分岐を選ぶ | SP-004 |
| TC-119 | unit | `SHA256SUMS.sig` は改ざんされた manifest を検証失敗にする | SP-014 |
| TC-120 | manual | diagnostics trace smoke: `RUST_LOG` と `FLISTWALKER_WINDOW_TRACE` の責務分離を維持し、対象 flow が canonical event family と相関 field を出す | SP-010, SP-014 |
| TC-121 | unit | Windows regression: `copy_selected_paths` の notice 正規化は `app.shell.runtime.notice` を更新し、旧 shell 直参照の残骸を検出する | SP-010 |
| TC-122 | unit | 回帰: Walker 完了後の kind 解決は表示中結果に限定し、巨大な on-demand root で全件 metadata 解決を継続しない | SP-007 |
| TC-124 | unit+perf | adaptive walker は GUI/TUI 共通の唯一の backend として同一 dataset の候補件数を標準 read_dir 走査と一致させつつ elapsed time と read_dir 制御指標を診断出力できる。adaptive limit は探索方向を保持し、減少で改善した場合はさらに減少、増加で悪化しない場合は増加探索を継続し、悪化時は方向反転する。Windows の Hidden+System+ReparsePoint 互換 junction は候補化せず、single-worker serial fast path は同じ候補を返して metrics を記録する | SP-002, SP-007, SP-016 |
| TC-125 | unit | `developer` runtime config は手動追記時だけ読み込まれ、自動生成 config seed には出力されない。自動生成 config seed には `walker_max_entries` / `history_persist_disabled` / `restore_tabs_enabled` / `emacs_keybindings_enabled` / `tab_pin_moves_to_next_row` が既定値で含まれ、既存 config に欠けている場合も読み込み時に補完される。`walker_threads` / `walker_backend` は既存 config から削除され、adaptive walker の initial/max limit 未指定時の既定と明示値を検証し、metrics summary を指定ファイルへ追記できる。`adaptive_limit_avg` と `adaptive_limit_change_count` を含む summary 形式を固定し、平均並列度の読み取り手順を残す | SP-002, SP-016 |
| TC-126 | unit+perf | adaptive walker の制御メトリクスは `adaptive_limit_final` だけでなく `adaptive_limit_avg` と `adaptive_limit_change_count` を出力し、再測定時に平均並列度・揺れ幅・最終値を比較できる。`adaptive_limit_avg` は shutdown/join 尾を少量含みうることを前提に解釈する | SP-002, SP-016 |
| TC-127 | unit | GUI 設定ボタンの handler は runtime config file を生成済みのパスとして解決し、既定アプリ open が失敗した場合にテキストエディタ相当の fallback handler を呼ぶ | SP-016 |
| TC-128 | unit | active indexing の `Finished` 応答時に未反映 entries が残っている場合、GUI frame は全件 drain せず、複数 frame に分けて吸収した後に terminal state へ遷移する | SP-010 |
| TC-129 | unit | Walker が上限打ち切り後に `Truncated` と `Finished` を返し、大きな pending entry backlog が残っていても、GUI は frame budget を維持しながら少数 frame で terminal state へ到達する | SP-010 |
| TC-130 | unit | 空クエリ・フィルタなしの active indexing は、表示更新時に `incremental_filtered_entries` と `runtime.entries` へ全件複製せず、`runtime.index.entries` から上位 `limit` 件だけを表示する | SP-010 |
| TC-131 | unit | active indexing の `Finished` 応答後に pending entry drain が残っていても、status line は `Indexing...` を表示せず、内部後処理 marker により repaint と terminal cleanup は継続する | SP-010 |
| TC-132 | unit | active indexing の terminal cleanup は、大規模 drain 後の `pending_entries` queue を同期的に shrink せず、後処理完了時の UI freeze を避ける | SP-010 |
| TC-133 | unit | notice 消去を含む通常 status line 更新は OS メモリ使用量を同期サンプリングせず、memory sampling は専用更新経路でのみ行う | SP-010 |
| TC-134 | unit | `Finished` 後の pending entry drain は探索中より小さい 1-frame 件数上限で分割され、notice 消去直前に大きな backlog を単一 frame で吸収しない | SP-010 |
| TC-135 | unit | フィルタ適用中の active indexing 完了時は、ingest 中に作った増分 filtered snapshot を terminal `entries` に再利用し、全候補を再走査しない | SP-010 |
| TC-136 | unit | active indexing 中の空クエリ・フィルタなし表示更新は、蓄積済み index entries を `runtime.entries` へ全件 clone せず、表示上限分の results のみ更新する | SP-010 |
| TC-137 | unit | kind filter 用 unknown path queue 構築は、既知 kind / pending / in-flight を重複投入せず、queue 対象 path だけを追加する | SP-010 |
| TC-138 | unit | 保存済み window 位置が現在の表示範囲外にある場合は起動位置を表示範囲内へ補正し、現在の仮想ディスプレイ内の負座標は維持する | SP-010 |
| TC-139 | unit | タブ close ボタンの hover 表示は、タブ accent または clear outline と同系色の背景・細い枠・カーソルで押下領域をタブ本体と区別できる | SP-010 |
| TC-140 | unit | `FLISTWALKER_DISABLE_SELF_UPDATE=1` では起動時更新確認を送らず、更新チェックも `None` を返して自己更新を無効化する | SP-014 |
| TC-141 | unit | runtime config の `emacs_keybindings_enabled=false` では `Ctrl+N` / `Ctrl+P` / `Ctrl+V` / `Alt+V` が current row を移動せず、通常の矢印移動と `PageUp` / `PageDown` は維持される | SP-010, SP-016 |
| TC-142 | unit | runtime config の `emacs_keybindings_enabled=false` では `Ctrl+J` / `Ctrl+M` が実行/オープンを起動せず、通常の `Enter` は維持される | SP-010, SP-016 |
| TC-143 | unit | runtime config の `emacs_keybindings_enabled=false` では検索欄フォーカス中の `Ctrl+K` など TextEdit 側の Emacs 風編集 chord も query を変更しない | SP-010, SP-016 |
| TC-144 | unit | `Ctrl+Shift+T` は現在プロセス中に閉じたタブを LIFO で active tab として復元し、可能な限り閉じる前の tab index へ戻し、復元対象がない場合はタブ構成を変更しない。復元タブは新しい tab id を持ち、古い pending request/routing 状態を復元しない。閉じたタブ stack は直近 25 件を上限にする。起動時復元された background tab を初回 activate 前に閉じて復元しても lazy refresh を実行する | SP-010 |
| TC-145 | integration | PowerShell build helper は `-CheckOnly` で machine state を変更せず、`-NoInstall` と非対話通常モードでは不足依存ごとの手動コマンドを表示して失敗する | SP-018 |
| TC-146 | integration | PowerShell build helper は Rustup、Rust GNU target、MSYS2、`mingw-w64-x86_64-gcc` を別々に承認し、固定 package ID/source と `pacman -S --needed` を使い、拒否・winget 不在・install failure・再検出 failure で build を開始しない | SP-018 |
| TC-147 | integration | Windows native GNU release build は strip 済みの `flistwalker.exe` / `FlistWalker.exe` を byte-identical に生成し、`.rsrc`、`asInvoker` manifest、console subsystem を保持し、意図しない MSYS2 runtime DLL に依存しない。通常の PowerShell 起動で batch CLI の完了と exit code を同期取得でき、GUI mode は native window 起動前に console から切り離す | SP-006, SP-018 |
| TC-148 | unit | Windows host の GNU resource build は resolved `windres` / `ar` を使用し、`resource.o` を `flistwalker` binary へ明示リンクする | SP-018 |
| TC-149 | unit | active indexing 中にタブ切替して request が background 化しても、切替前に取り込み済みの entries、未 drain の pending entries、切替後の background batch を同じ完了 snapshot に統合し、`ReplaceAll` では切替前 partial を破棄する | SP-010 |
| TC-150 | unit | action executor は固定 2 worker + queue 8（総量 10）を超えて受理せず、UI の non-blocking dispatch は受理成功後だけ pending/routing/in-progress を commit する。`Full` は直前の受理済み action state を維持し、`Disconnected` は今回要求を pending 化せず terminal failure に settle する | SP-010 |
| TC-151 | unit | kind resolver は固定 1 worker + queue 256（総量 257）を超えて受理せず、`Full` の path を queue 先頭へ戻す。最新 tab/epoch 判定を metadata I/O より先に行い、stale、tab 消失、共有状態 poison、`Disconnected` を filesystem I/O 0 件の `kind=None` terminal response として pending/in-flight から解放する | SP-010 |
| TC-152 | unit+integration+regression | index executor は stale を含め固定 2 worker + queue 2（総量 4）、coordinator tracking は最大 2、app pending は全体最大 4 かつ tab ごとに最新 1 件を維持する。worker channel に受理された request は replacement 後も terminal response まで tracking に残し、stale 非終端 response では解放せず、送信成功時だけ inflight に加える。`Full` は latest-only pending へ戻し、stale/cancel は canonicalize/FileList/walker I/O より前に `Canceled` response、`Disconnected` は terminal failure として routing/in-progress を解放する。active-tab 優先 preempt または app pending queue eviction で replacement のない background tab は activation refresh marker を再設定し、再 activation で replacement を要求する。queue eviction は active request とより新しい同一 tab replacement を保護し、旧 terminal は新 request state を消さない。production worker の最新 request は FileList discovery の cancel predicate を反転して扱い、小規模 local FileList の startup/Refresh Index が bounded time 内に `Started(FileList)`、実 entry、`Finished(FileList)` を返して app の Source/Entries へ settle する | SP-001, SP-010 |
| TC-153 | unit | action/kind/index の queue/in-flight counter は accepted request だけを RAII guard で計上し、正常、error、cancel、stale、disconnect、panic unwind で 0 へ戻る。scheduler lock 外で I/O と response/log を実行し、action 2・kind 1・index 2 の名前付き direct handle を stop accepting -> drain/cancel -> join -> response close の順に全体 250ms 以下で shutdown し、worker/correlation/outcome と queue/in-flight を trace する | SP-010 |
| TC-154 | unit+perf+manual | non-sparse および sparse な大規模 tab fixture の通常切替は index/pending/kind/incremental/base-result/result container と entry-kind cache の allocation identity/capacity を outgoing slot と incoming shell の両方向および switch-back で維持し、要素単位 clone/drop、capacity 縮小、activation 後の全件 cache/result rebuild を行わない。restore lazy refresh は Query 空/非空 × FileList/Walker の全組合せで同じ遷移中に indexing state と request を確立する。index/search/sort/preview の request ownership を破棄して閉じた tab は、新tab IDへ旧routingを移さず、indexはactivation refresh、search/sortは保持snapshotからの同種request、previewは選択path reloadとして復元する。indexとresult workの同時中断ではnonempty search、empty-query AllMatches sort、ShownResults metadata sortをreplacement indexと併せて再発行し、index成功後の最終snapshotへsort intentを再適用し、index失敗後も新tab IDのresult requestを維持する。固定10万件の release-mode coordinator fixture は25回以上でp95 16msを目標、50msをhard ceilingとする。active indexing の `Batch` / `ReplaceAll` / `Finished` と search/preview/action/sort の superseded routing が current active tab ID を指しても active scratch を更新しない。新規タブは生成前に dirty query history を確定し、create/close/restore/reorder/session persistence は tab identity と active/background routing を維持する。native GUI では大規模候補の反復切替中も入力・スクロールを継続できる | SP-010 |
| TC-155 | unit+integration | shared compiled query は公開 query/search/highlight adapter の empty/error 契約を維持し、whitespace AND、`'`、`!`、`^`、`$`、token 内 `|`、mixed exact OR、repeated exact、lone/empty operator、case、relative/absolute path、regex/plain-in-regex、multibyte character span を同一解釈で評価する。公開 visibility は mixed positive+exclude でも positive term だけを投影し、公開 highlight は除外一致または別 positive term 不成立でも成立済み positive span を投影する。authoritative search の match/score/order、projection span は characterization fixture と一致し、sequential/parallel/indexed/prefix-warm が同じ path/score/order を返す。query は search request と highlight scope ごと、ignore terms は CLI operation と GUI scope/pass ごとに1回だけ compile される | SP-003, SP-009 |
| TC-156 | perf | 計測区間外で構築した固定 10万件 fixture を release mode で selective fuzzy、multi-token AND、exact、inverse/exclusion、anchor、OR、regex の sparse/dense shape と unknown-kind `ext:` shape により5回以上測定し、compile、cold、warm、評価候補数、match 数、median、maximum を報告する。代表 cold/warm median は 100ms 未満を目標とし、weekly CI の全 shape は各 sample と maximum が 250ms 未満、warm は cold と同じ path/score/orderで評価候補数を削減する。関連 unit test は同値 file/directory projection の kind probe 0 件、TUI snapshot 再利用、superseded GUI/TUI request の協調 cancel と partial response 0 件を固定する | SP-007 |
| TC-157 | unit+integration | updater staging は manifest/signature を binary/sidecar より先に取得し、strict manifest の必須 digest 一意性を検証する。release JSON 2 MiB、manifest 1 MiB、signature 64 KiB、binary 512 MiB、sidecar 16 MiB、接続/無通信/request/全体 deadline、3-hop redirect/origin policy を `Content-Length` 欠落・過少申告を含む streaming fixture で強制し、partial/error/timeout 後はこの要求が作成した staging だけを cleanup する | SP-014 |
| TC-158 | unit+platform | `VerifiedUpdateBundle` の activation 準備は executable parent 内の create-new `.new`/lock/marker/backup に限定し、directory/symlink/reparse/parent 外/衝突を拒否する。各操作直前に prepared new hash と既存 target old hash を再照合する。sidecar を先、binary を最後の commit point とし、Windows existing target は updater process 内の native `ReplaceFileW`、Linux existing target は同期済み backup + same-directory rename、不在 target は racing destination を上書きしない hard-link promotion を使う | SP-014 |
| TC-159 | unit+integration+regression | deterministic failure injection は per-target write-ahead state、pre-marker orphan lock/evidence retention、parent/helper registration と acknowledgement の全 crash point、ack 前 no-mutation、live parent/helper の artifact-mutation 前 exclusion、helper executable path/hash/token identity、30秒 parent wait の fail-closed 判定、各 sidecar/binary 操作、restart failure、startup recovery、marker/artifact type・hash cleanup validation、invalid transition/ambiguous-state retentionを検証する。通常startupはlive helper identity mismatchを`Ambiguous`とし最大5秒で`Deferred`となる一方、内部Headless restart childはvalidated terminal markerのhelper PID終了をidentity照会なし・artifact無変更で最大30秒待ち、終了後にmarker/hashを再検証してcommitted transactionをcleanupする。rollback 開始は `rolling_back` を先行永続化し、その途中の中断から再開する。precommit/restart failure は旧 bundle、committed state は完全な新 bundleへ hash 検証付きで収束する | SP-014 |
| TC-160 | platform | copied Rust helper core と Windows native `ReplaceFileW` / Linux synchronized rename adapter は、同一 filesystem 上の inert dummy transaction で成功、precommit failure rollback、restart failure rollback、中断 recovery、atomic primitive、cleanup を実証する。実行中 FlistWalker binary、production update feed、外部 application を置換・起動しない | SP-014 |
| TC-161 | unit+perf | root/階層/祖先 FileList は UTF-8 と先頭 UTF-8 BOM、LF/CRLF、非 ASCII path を同一解釈で扱い、writer は UTF-8 BOM なしで round-trip する。UTF-16LE/BE BOM、legacy 不正 byte、NUL、途中切れ UTF-8、1 MiB 超/no-newline 行は path と offset/limit を含む明示 error となり、安定した root 入力は callback 0 件、invalid child は親 subtree 不変、invalid ancestor は no-rewrite、長行途中 cancel は 64 KiB 以下の cadence で停止する。VM-003 の allocating/probe control は production と同じ preflight を使い既存 threshold を維持し、validation-only と total parse elapsed を別々に記録する | SP-001, SP-007 |
| TC-162 | unit+integration+platform | interactive CLI は stdin+stderr TTY / redirectable stdout の ownership、stderr-only ANSI、guard 解放後 stdout、partial setup/draw/read error/unwind 復旧、request-id freshness、selected-path preservation、ordered hidden pin、empty Enter、cursor editing/paste/repeat、dynamic viewport/resize、Unicode clipping、batch/TUI cancellation exit 130、bounded worker shutdown を検証する。各描画 frame は complete payload を terminal synchronized update の開始・終了 command で囲み、全画面 clear の中間状態を表示させない。runtime config の `emacs_keybindings_enabled` が navigation/page/pin/select と normal query / history filter の editing chord を一括で有効・無効にし、help と各 overlay が有効な chord だけを表示すること、`tab_pin_moves_to_next_row` が Tab/Shift+Tab/Ctrl+I の toggle 後移動を制御することも検証する。TUI Walker が adaptive initial/max limit と `walker_max_entries` を適用し、cap 到達時に最新 request だけへ truncation notice を finish 前に返して検索結果更新後も保持する一方、FileList 作成は完全 fresh snapshot を維持することを検証する。既定Autoと明示Walkerはmain thread discoveryを0回としてworker ownershipへ渡し、Auto discoveryはcancel可能とする。`--source filelist`のmissing root FileListはterminal開始前のbounded同期preflight（terminal cancellation前の限定例外）で非ゼロ終了し、成功時の単一結果をfirst workerが再探索せず消費する。明示Walkerはindex後もFileList存在をunknownに保ち、F6でcancel可能な遅延discoveryを行い、既存ならsnapshot生成前にoverwrite確認、未存在ならcreateへ進む。discovery中のF4 root switch/SelectOutputはsuccess/cancel/failureすべてで一度だけsettleし、pending intentを後続creationへ残さず、stale request IDは現行stateを変更しない。missing/invalid root は `IndexFailed` だけを返し、candidate batch append は既存 path allocation を共有し、同一 snapshot/root/ignore scope の連続検索は projected entries を再利用して prefix cache の live identity を維持し、worker response drain は iteration budget を超えない。pseudo-TTY で自動化できない terminal case は Windows 実端末で証跡を残す | SP-006, SP-008, SP-016 |
| TC-163 | unit+integration | batch CLI は `--sort` の全 mode、sort-before-limit、`limit=0`、root selector mutual exclusion、saved-root one-based listing、legacy print framing、index/search の段階別 progress を検証する。interactive startup は saved/default root、初期 sort、初期 ignore state を反映し、`--no-ignore` でも terms を保持して TUI から再有効化でき、batch 専用 progress/fail-no-match を usage error にする。valid `--list-saved-roots` は runtime config bootstrap 後も index/action/write 0 回、invalid list arguments は bootstrap 前に exit 2、invalid root selector は indexing 前 exit 2 とする | SP-006, SP-013, SP-016 |
| TC-164 | unit+integration | action request は trusted root/current-row/request identity/cancel を保持し、whole-request preauthorization failure と observed cancel/root switch 後の backend call を 0 件にする。batch action は print/open/reveal、`--action-all` guard、stdout/stderr framing、exit 2/1/130、TUI action は pins に関わらず current row only を recording backend で検証する | SP-004, SP-006 |
| TC-165 | unit+integration | FileList write plan は root detection precedence、both-name/case-variant の deterministic target、overwrite/ancestor consent 0 write、precomputed target set、read-only/invalid ancestor/symlink、per-replacement cancel、partial failure/cancel rollback と stderr-only transaction report を temporary root で検証する。batch `--create-filelist` は write dispatch 前に runtime config bootstrap を完了する | SP-001, SP-006, SP-016 |
| TC-166 | unit+integration | TUI FileList confirmation/active state は cancel、delayed worker（250ms 超）、commit 後 cancel、rollback failure、panic/disconnect、pending intent priority、worker join、return 後 write 0 件を検証する。settled rollback failure は selection/root retry を保持し、CancelExit は terminal restore 後 exit 1 とする | SP-001, SP-006 |
| TC-167 | unit+integration | history persistence は disabled no-op、two serialized writer の delta merge、same-process `A,B,A` recency、cap 100、unknown top-level/nested field preservation、lock timeout retry、write 中 enqueue generation を検証する | SP-016 |
| TC-168 | unit | persistence worker の frame dispatch は lock wait と独立し、non-history leaf の last-write-wins と ordered history concat を保持する。graceful bounded flush と crash-before-flush residual risk を test/report に区別して記録する | SP-016 |
| TC-169 | unit+integration+headless | TUI update notice は英語の `Update available` と終了後の `flistwalker --update` 手動実行案内を含み、check result の受信だけでは apply を開始しない。`--check-update` / `--update` は独立 CLI 操作として help に英語で掲載され、alias 互換の `--cli --update`、display server 環境変数なしの内部適用確認、GUI/CLI 別 restart mode、更新確認無効時、up-to-date、candidate、manual-only、check/apply failure の出力と exit code を検証する | SP-006, SP-014 |
| TC-170 | unit+integration+platform | `-x` template は固定 command 後の standalone `{}` 1個を要求し、全 post-limit/interactive selected path を canonical absolute argv として順序どおり渡す。Windows direct `.bat` / `.cmd` 拒否と明示的 `cmd.exe /C` opt-in、OS argv budget の greedy batch と実行時 argument-limit 細分化、`--exec-max-args` cap、dry-run、target 0 no-spawn、root escape/reauthorization、spawn/child failure と cancel の後続停止、stdout empty/stderr report、legacy option conflict、terminal cleanup-before-spawn を recording backend と platform command で検証する | SP-006, SP-008, SP-009 |
| TC-171 | unit+platform | Windows updater の既存 target 置換は `PATH` から PowerShell を除外しても成功し、native `ReplaceFileW` の backup あり/なし、target 欠落時の source/backup 不変、verbatim prefix を保持した NUL 終端 UTF-16、interior NUL 拒否を検証する。production binary は置換しない | SP-014 |
| TC-172 | unit+integration | CLI は `--color[=auto|always|never]` を受理し、未指定時は色を出力しない。batch の `--color`/`auto` は TTY のみで、一致文字列だけを色付けし、pipe/redirect の path-only stdout framing を維持する。`NO_COLOR`、`always`、`never` と interactive CLI の画面描画への適用を検証する | SP-006 |
| TC-173 | unit+headless+manual | GUI の `Help` と `F1` はショートカットとquery syntaxのモーダルを開き、`F1` / `Esc` / `Close` で閉じる。表示内容は OS primary modifier と `emacs_keybindings_enabled` に加え、`name:` / `path:` / `dir:` / `ext:`、field演算子、複合例を反映し、表示中のコピー等の背景ショートカットは状態を変更しない。headless snapshot と GSM-008 で表示契約を確認する | SP-010 |
| TC-174 | unit+integration+headless+manual | catalog name validation、unknown-field round trip、newer/malformed read-only failure、sidecar-lock RMW、legacy roots 不変、named-root snapshot fallbackを検証する。CLI は management/apply と positional `root`/`preset`/`catalog` query 互換、TUI startup preset を検証する。GUI は picker open/filter/select/apply/cancel、現在tabからのpreset add draftとatomic add、preset全pure-search field編集・rename/collision・保存/破棄、確認付きdeleteとatomic remove、mutation成功・失敗時のdraft/確認保持、現在tab不変に加え、Preset root と Named Root path のテキスト入力、folder picker の開始位置・選択・cancel・failure時draft保持、Named Root管理画面のadd/edit/rename/delete、absolute path検証、rename時の参照preset追従、delete時のsnapshot fallback、unknown-field保持、最新response、nested modal input遮断、F1 help、メイン画面にpreset controlがないことをunit/integration/headless snapshotとGSM-008で確認する。同一root・非空queryでは旧request cancel、filter済み最新snapshot、preset sort、tab sort scope、stale response拒否、最新response後のsort維持、Ignore List無効化時の候補復元を確認する | SP-019 |
| TC-175 | unit+integration+perf | `name/path/dir/ext` のpositive/exact/exclusion、anchor、OR、regex、case、multibyte highlightを共有compiled queryで検証する。未知prefixと非field queryの互換、空field error、separator正規化、dotfile/directory/複合suffix、Entry kind、prefix cache非適用、CLI/TUI/GUI共通結果、10万件query-shape性能を確認する | SP-020 |
| TC-176 | unit+integration | ignore decoderはUTF-8 BOM、CRLF、`!`付き先頭termを正規化し、ignore matcherは `/` / `\\` separator差を吸収する。staged CLIの既定sidecarと明示 `--ignore-file` をWalker/FileList・空/非空queryで検証し、missingは空term、存在するinvalid UTF-8はCLI/TUIで明示error、TUI startupは同じterm列を保持する | SP-015 |
| TC-177 | unit+regression | Windows TUI の options summary、root切替・refresh status、root picker、FileList必須errorはdrive/UNC extended prefixを共有表示境界で除去する。TUI本番sourceで直接 `.display()` / `to_string_lossy()` を使う変更は静的guardで失敗させる | SP-006 |
| TC-178 | integration+regression | Windows release ZIP は `flistwalker.exe`、`README.txt`、`LICENSE.txt`、`THIRD_PARTY_NOTICES.txt` の4項目だけをarchive rootへ格納し、flat release asset用のversion付きsidecar名をarchive内へ漏らさない | SP-012 |
| TC-179 | unit+platform+regression | updater helper command は installation directory を child current directory に固定しない。Windows verbatim drive/UNC helper path は UTF-16 identity を保った非 verbatim path も各 retry ラウンドで試し、全試行失敗の診断は OS error を保持しつつ `\\?\` prefix を表示しない。失敗した preparation の cleanup 後は fresh transaction を開始できる | SP-014 |
| TC-180 | unit+integration+GUI+perf | max depth の境界（root直下=1、上限directoryは含み子を走査しない）、Walker/FileList/Auto一致、root外FileList除外、CLI引数/競合、TUI起動伝播、GUI active-tab-only適用・表示・再index、有限値からUnlimitedへの復帰、popup/preset editorのチェックボックス中央揃え、有限・unlimited preset適用と他tab非波及、preset/session round-trip、新規tab unlimited、FileList作成非適用を検証し、depth判定が追加metadata probeを導入しないことを確認する | SP-021 |
| TC-181 | unit | stateful event generator は同じ seed/profile から同じ event 列を生成し、失敗時は seed、step、event、semantic state digest、再生コマンドを出力する。invariant checker 自体は破損 snapshot fixture により tab 0件、重複ID、active範囲外、result/count不整合、current row範囲外、queue/in-flight上限超過を検出できる | SP-022 |
| TC-182 | unit | 固定 regression corpus と複数 seed の操作列は create/close/restore/switch/reorder、query/root change、index/search response を production transition API 経由で適用し、各 step 後に live tab identity、active owner、routing、result/selection、queue bound を維持する | SP-022 |
| TC-183 | unit | index/search の batch/replace/finish/fail/cancel、preview/action の success/failure、sort metadata の resolved/unavailable、FileList の finish/fail/cancel と stale/out-of-order completion を sequence 中へ挿入しても closed tab または別 tab を変更せず、quiescence は固定 step 以内に pending/in-flight/routing/progress state を解放する | SP-022 |
| TC-184 | unit+integration+scheduled | ignored extended profile は指定 seed/count/steps を再生でき、実 worker soak は一時 root 内だけで index/searchとtab/root/query変更を反復して期限内に収束する。専用 non-required workflow は固定 tool/action、runner image evidence、timeout/concurrency、workflow dispatch、失敗ログ artifact を持つ | SP-022 |
| TC-185 | perf+scheduled | 1,000,000件の固定候補を計測外で構築し、selective-fuzzy/dense-fuzzyを空cacheから各7回実行する。各 repetition の件数・path・score・order一致をassertし、nearest-rank p50/p95/p99と`before_fixture`/`after_fixture`/`peak_search`/drop後最大1秒quiescenceの`after_drop_quiescence` RSSをstable `tc_185` labelで記録する。RSSはobservational-onlyとし既存10万件hard limitを変更しない | SP-007 |
| TC-186 | unit+platform+regression | Windows updater restartは通常/verbatim pathで最大3ラウンド・100ms間隔のbounded retryを行い、verbatim pathは各ラウンドで非verbatim表現も試す。GUI childの500ms以内の終了は失敗として扱う。新版起動失敗時は旧bundleを検証して同じ契約で旧GUIを再起動し、新版・旧版の両起動失敗時は両errorを保持する。production binaryや外部applicationは起動せず、process launch seamとrecording backendで検証する | SP-014 |
| TC-187 | unit+platform+regression | Windows GUI の console detach 後を模した隔離 subprocess で stale stdin/stdout/stderr を設定しても copied helper を起動できる。hidden helper/restart command は stdio を `NUL` へ固定し、通常 helper path と verbatim/non-verbatim path pair の一過性失敗を最大3ラウンド・100ms間隔で回復する。production binaryの置換や外部applicationの起動は行わない | SP-014 |
| TC-188 | unit+GUI+regression | update の共有 atomic state は cancel-before-handoff と handoff-before-cancel を CAS で決定し、native/signal close は release read、download、再hashを含む staging/verification 中に同じ token を cancelしてviewport closeを保留する。無効candidateはstaging directoryを作らず、handoff開始後はhelper acknowledgementまで取消を無視し、terminal/disconnect response後だけcloseを再送する | SP-014 |
| TC-189 | unit+filesystem+GUI | helper failure diagnostic は検証済み transaction context の固定 path へ versioned/bounded/sanitized/no-overwrite record として保存し、symlink/reparse/wrong type/oversizeを拒否する。起動時に一度だけ消費し、抑止可能な check failure と別の `Previous Update Failed` modalへ表示する | SP-014 |
| TC-190 | unit | OS/CPU support は asset lookup より先に判定し、Windows/Linux x86_64だけが install payloadを持つ。macOS/unsupported architectureはx86 assetへ仮装せず、release metadataだけのmanual-only candidateになる | SP-014 |
| TC-191 | integration+platform+CI+regression | test-channel公開鍵を埋めたWindows GNU artifactをWindows runnerへ渡し、実行可能なまま明確にhashが異なるoverlay付きupdate payloadを作成し、全取得先をloopback HTTP、署名をtest keyだけに限定したheadless sandbox更新をUniversal/Fwの順で実行する。mixed feedの対象/非対象family assetは異なる有効payload/hashとしwrong-family選択を最終binary hashで失敗させ、fresh sandboxへseedしたcounterpart binary/sidecarの不変hashも確認する。各invocationはcaller signing keyを保持する一方、sandbox app用`ProcessStartInfo`からkeyを除外してhelper/restarted childへ継承させない。更新前後で異なるbinary hash、sidecar、marker/lock消滅、restart child終了をtimeout付きで確認し、server traversal、sandbox子孫のreparse cleanup、signing key子process継承、caller key破壊、job削除/skip/test keyのproduction混入をpolicy negative testで拒否してCI Gateへ集約する | SP-014 |
| TC-192 | unit+GUI+worker | 保存済みrootのAdd/Edit検証は専用workerがcanonicalizeとdraft snapshot重複判定を行い、request_id、dialog generation、intent/indexが一致し、入力・選択・remove状態が後から変化していない最新responseだけを反映する。close/reopen、stale success/error、disconnect後もUI threadにfilesystem I/Oやpending残留を持ち込まない | SP-010 |
| TC-193 | unit+integration+release+perf | `fw` の短いprogram name、暗黙CLI、hidden updater先行、universalとのstdout/stderr/exit parity、uncached one-shot ranking、両Windows resourceを検証する。updaterはvariant別assetを選び欠落時にfail closedとし、version付き/rename済み`fw`の更新でも`fw.`付きローカルsidecarへ分離して同居するuniversal sidecarを保持する。release validatorは既存archiveのmember集合を厳密照合し、28 asset/26 checksum entryを確認する。Windows release buildの固定shallow 200-file fixtureを5 warmup+25 sampleで測定し、`fw` medianがuniversalの70%以下であること、CLI open/revealに必要なShell32/User32を除きGDI32/OpenGL32/imm32/psapi/dwmapi/uxthemeのGUI framework/rendering/window系importがないことを確認する | SP-023 |
| TC-194 | unit+regression | `SHA256SUMS` に `FlistWalker-*` と `fw-*` が混在する v0.24.3 相当の manifest を strict parser が受け入れ、`fwx-*` の近似 prefix は拒否する | SP-014 |
| TC-195 | unit+GUI | preset適用と履歴検索の確定・キャンセルでqueryを置換した後、TextEdit cursorが新query末尾へ移動しfocusが検索欄へ戻る | SP-010 |
| TC-196 | script+release+regression | 直前の公開版updater contractが、それよりstrictに新しいcandidateの`SHA256SUMS`を受理する。checkerは公開生成順のexact 26-entry inventoryとlegacyのASCII 64hex + 正確な2 space + safe family basename文法を再現し、v0.24.3 contractで23行目の最初の`fw-*`行をrow番号付きで拒否し、v0.24.4 contractで隣接candidateを受理する。同一/過去candidate、空manifest、重複、separator、近似prefix、空白・hash異常、構文不正version、形式が正しくてもcapability未登録のversion、compatibility acknowledgementをfail closedとする。Guardianで不変化されたpolicy testは通常PRのcandidate checkerを独立golden fixtureで直接実行する。trusted CI policyはlatest published release取得、N-1 checker invocation/必須引数、no-bypass契約を守り、全N-1非互換をrelease blockerにする | SP-014 |
| TC-197 | unit+GUI | thread-local/call-isolated seamで10,000件Resultsの描画処理行数がviewportのvisible rangeにboundedであること、2 frame後にoffscreen current row 9999が実際に描画/選択されること、visible行のclick/double-clickが絶対index/actionを維持することを外部actionなしで検証する | SP-010 |
| TC-198 | unit+CI+release+regression | heavy PR CIはLinux clippy/coverageに加えてmacOS/Windows native jobで`cargo clippy --locked --all-targets -- -D warnings`を明示的な安全な`bash` shellで実行し、tag release preflightはLinux/macOS/Windows native全jobでlocked test/clippyを再実行する。trusted policy checker/testは対象job内のblocking stepを一意かつ許可されたプロパティ行の完全一致で要求し、PR側の非Linux条件/command欠落、release側のplatform限定condition、`--locked`/`--all-targets`/`-D warnings`欠落、`continue-on-error`、`|| true`やcustom shell/envなどのfailure握り潰し、別jobへの移動、step重複を拒否して、platform cfg固有のunused/dead code warningをtag作成前とasset build前にblockする | SP-012 |
| TC-199 | unit+GUI+regression | `emacs_keybindings_enabled=true` かつ `ctrl_w_deletes_word_in_query=true` の GUI 検索欄・履歴検索フィルターへフォーカス中は `Ctrl+W` が直前単語だけを一度削除しタブを閉じない。設定無効、Emacs無効、検索欄外では従来のタブ終了を維持し、IME合成中は削除も終了も行わない。新設定は既定falseで既存configへ補完され、明示trueを読み込める。TUIの既存単語削除は変更しない | SP-010 |
| TC-200 | unit+GUI+regression | 更新候補の承認後に manifest、signature、checksum、network、または activation が失敗した場合、提示中の `Update Available` を消し、現在 installation 不変、失敗詳細、release URL、同一 variant の binary と `SHA256SUMS` を使う手動復旧案内を専用 failure modal へ表示する。失敗後に同じ候補を `Update Available` として再表示せず、startup の check failure modal とは状態を分離する | SP-014 |
