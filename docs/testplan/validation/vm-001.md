# VM-001: Docs only

Back to the [Validation Matrix](../validation-matrix.md).

Read the [docs-only intent checklist](../validation-matrix.md#docs-only-or-sddtdd-document-updates) as well as this baseline.

## Typical Targets

`docs/*.md`, `AGENTS.md`, release note text only

## Required Validation

affected doc diff review, `rg` で ID/参照整合を確認

## Optional, Manual, Or Follow-Up

Rust 実装に触れない限り `cargo test` は不要
